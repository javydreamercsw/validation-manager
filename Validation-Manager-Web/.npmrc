# Use the public registry for this project. Local machines may point npm at
# an internal mirror whose tarball URLs break npm installs (ENOTFOUND on
# tarball fetches); CI and other contributors use the default anyway.
registry=https://registry.npmjs.org/
